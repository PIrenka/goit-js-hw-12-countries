const searchContainerRef = document.querySelector('.search-container');
const inputRef = document.querySelector('.input-searchCountry');
const btnRef = document.querySelector('.input-btn');
const countryMarkupRef = document.querySelector('.country-markup');

export { searchContainerRef, inputRef, btnRef, countryMarkupRef };
